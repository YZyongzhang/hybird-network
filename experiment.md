### time oct 28 16.24
保存了twoframe的版本，之后所有的实验都基于twoframe的版本。该时间运行了生成lstm数据的实验。序列长度为5.使用滑动窗口。预计准备输入到lstmoffline代码中去。offline v4 是离散的saclstm
/dataset/pt/offline/pt/v1 为lstm v2为twoframe
### time 10 29 
在昨天夜里跑的level曲线中，我察觉到在最开始的持续上升阶段能够很大的成功。但是reward曲线无法稳定。我觉得这个地方只有两个问题。1. q网络没有学到东西，我觉得是cql打压过于厉害。查找之后发现，这里设置的1.改成0.2后跑一次实验。2. 数据内部有很多的错误数据。我进行可视化之后，排除了收集的数据的问题。后面如果谁数据的问题，就可能出现在pt生成的时候有问题，或者是在网络内部reshape的时候没有对齐。但是我目前发现offline可以学到东西，就说明数据内部肯定是有正确的。那在这种情况进行反推，就能得到reshape肯定没有问题（如果有问题所有数据都是错误的。）。所以可能pt有问题？比如在后续的循环过程中，某些数据累计了。或者是cat错误了。验证这个就很简单，直接使用第0个pt文件就可以了。如果情况有上升，那么就是因为数据的问题。如果没有，那就是算法比如cql的问题。在之后的实验中，统一设置为500个epoch
1. 首先跑cql的实验。"offlineRL/loss/v4model/level/3" cql 0.2 实验结果反而不如第一次了。说明不是cql的问题。将cql改回来，再跑一次进行对比实验
2. 将cql去掉。发现无法复现昨天晚上的实验。曲线更差，甚至spl没有成功的例子。所以目前来说我应该是改了其他的参数。
3. 通过git diff 进行判断，只改了一个target_actino_gap 但是这个参数在网络中应该是没有起作用的。针对这个事情。我将该参数改回。进行跑一次实验。
4. 3跑的实验debug来看，再一次出现了熵坍塌。根据经验，熵坍塌有两种情况，一种是数据出现了问题，一种是某些神经元死亡。从之前的实验转战，准备进行下面的两个实验1. 改成每10个跑一个epochreward。 2. 从内部看一下是不是数据在某些地方出现的问题。这里第一个cql的实验还没有跑通。
5. 根据四，我设置每1个epoch跑一次，最后发现依旧和之前那个最好的版本有很大的差距。从代码上来看，没有其他修改的了。所以应该不是代码的问题。ok，那现在问题就很明了。很有可能是昨天跑的数据和今天的是不一样的。也就是说数据在某些地方可能会造成更改。从这种不可复现的实验来看，我觉得可能出现的问题就是数据生成的时候有问题。我们把重心放到数据方面。跑一个实验只有第一个shard的
6. 第一个shard也不行。从生成数据大的角度，我突然好像发现了一点点的问题.ok,这个没有问题，我以为我们的1思路是取最后一个时间步，所以要用最后一个时间步的信息。但是好像不用，因为lstm输出的时间序列就是每一个步都带有前面时间步的信息。所以这个应该是合理的。
7. 综上所述，最后结果是算法没问题（昨天实验的偶然性）。数据也没有问题。
8. ok。 基于目前的代码进行调试
9. 我切换到昨天的分支上进行训练试一试



